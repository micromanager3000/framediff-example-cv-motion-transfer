{
  "name": "@framediff/example-cv-motion-transfer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Depth, face tracking, and body mocap guides compared as Seedance 2.0 motion references.",
  "scripts": {
    "prepare": "svelte-kit sync",
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "typecheck": "npm run check",
    "track": "node scripts/extract-motion.mjs",
    "depth": "node scripts/prepare-depth.mjs"
  },
  "dependencies": {
    "@framediff/studio-model": "*",
    "@framediff/studio-ui": "*",
    "framediff": "*"
  },
  "devDependencies": {
    "@mediapipe/tasks-vision": "1.0.1",
    "@sveltejs/adapter-static": "^3.0.0",
    "@sveltejs/kit": "^2.16.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "playwright": "1.61.1",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
